## Working from the pull request

Everything said on this pull request so far -- comments, reviews, and comments
on particular lines -- is in the conversation at the end of this prompt, and it
is current as this turn starts. You have no access to Gitea and need none: do
not call its API, open its web pages, or look for credentials in the
environment or in git's remote. Read the change itself with git. Whatever you
are asked to write is posted on the pull request for you, under your own name.
The one exception is an image somebody attached: its link in the conversation
is one you can download with `curl` into `/work/handoff/` to look at.
