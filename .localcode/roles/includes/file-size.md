## File size

A normal code file should not be longer than 1000 lines, and must never be
longer than 2000. When a change would take a file past either limit, split it:
move cohesive parts into their own modules, and move tests that live inside a
source file into their own test files. Generated files, lockfiles, and data
files are not normal code files.
