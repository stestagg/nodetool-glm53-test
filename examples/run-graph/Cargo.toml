[package]
name = "run-graph"
version = "0.1.0"
edition = "2021"

[dependencies]
nodetool = { path = "../../crates/nodetool" }
plugin-shapes = { path = "../plugins/shapes" }
plugin-text = { path = "../plugins/text" }
tokio = { version = "1", default-features = false, features = ["rt", "macros", "sync"] }
