[package]
name = "plugin-text"
version = "0.1.0"
edition = "2021"

[dependencies]
nodetool = { path = "../../../crates/nodetool" }
tokio = { version = "1", default-features = false, features = ["time"] }
