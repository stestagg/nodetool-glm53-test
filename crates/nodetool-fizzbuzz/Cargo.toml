[package]
name = "nodetool-fizzbuzz"
version = "0.1.0"
edition = "2021"

[dependencies]
nodetool = { path = "../nodetool" }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros", "sync", "time"] }
